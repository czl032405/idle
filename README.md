# IDLE

```bash
#dev
mongod
cd idle-server && npm run dev
cd idle-client && npm run watch 

#dev-only-client, proxy to idle.my-egg.me/api
cd idle-client && npm run dev 

#pro
cd idle-server && npm run start 
```